# propertyConnect Frontend

Next.js frontend for the `propertyConnect` property management application.

## Getting Started

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

The login form posts to `/api/auth/login` and expects a token-based response from the backend.
